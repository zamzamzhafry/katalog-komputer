# Data

> Supabase schema/types, storage, image strategy, SSR fetch.

- `2026-06-26` — @claude — Data baseline. **Supabase**: SSR client (createServerClient @supabase/ssr) in middleware (session refresh) + lib/supabase/{client,server}.ts. `database.types.ts` defines `Database` type. `lib/supabase/catalog.ts`: `fetchCatalogServer()` → `{ items, error }`, `mapRow` row→CatalogItem. `lib/supabase/auth.ts`: signIn/signOut/getUser. `lib/supabase/admin.ts`: service-role admin ops (delete/upload). **CatalogItem fields**: ProductID (string, key), Kategori, Brand, Model, Processor, RAM, Storage, Harga (number, rupiah), Stok (number), FotoURL (string), Deskripsi. **Image strategy** (README + CardImage in catalog-modal.tsx): fallback chain per item — (1) FotoURL, (2) `/img/{ProductID}.webp`, (3) `.jpg`, (4) `.png`, (5) placehold.co placeholder. `next/image` with `unoptimized` (all images unoptimized — remote + local mix). onError advances index. `public/img/` has 7 assets: ACC-001.webp, ACC-002.jpg, LAP-001.jpg, LAP-002.jpg, LAP-003.png, PC-001.jpg, PC-002.jpg. **Storage**: Supabase Storage for uploads via `uploadCatalogImage` Server Action (productId, file, currentPath) — writes DB + rollback orphan + cleanup old. currentPath currently always null (ponytail admin-dashboard:78 — old-file cleanup skipped). **localStorage** (lib/storage.ts): read-only offline cache of items. useCatalogData: if SSR empty, read cache; persist on items change. **SSR**: app/page.tsx force-dynamic, fetchCatalogServer server-side, pass initialItems+initialError to CatalogApp. dashboard/page.tsx same + getUser guard. **WhatsApp**: NEXT_PUBLIC_WA_NUMBER env (default 628123123123), redirectToWhatsApp builds wa.me URL with product detail message. **Env**: .env.local (real Supabase creds, gitignored), .env.local.example. -> lib/supabase/, lib/storage.ts, app/actions/catalog.ts
