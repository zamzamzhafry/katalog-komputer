# UI

> Design system, palette, components, animation, a11y, mobile. Primary bucket for upcoming UI/UX pro-max pass.

- `2026-06-26` — @claude — UI baseline indexed for pro-max pass. **Palette** (app/globals.css `:root`): dark, `--bg #0a0e14`, `--panel #11161f`, `--panel-soft #151b26`, `--ink #e6edf3`, `--mono #8b98a9`, `--line #1c2330`, `--accent #4f9cf9` (blue), `--price #ffd479` (amber), `--good #3fb950`, `--warn #f4704d`. Fonts: Inter (body) + `--font-mono` JetBrains Mono (IDs, specs, prices). **Identity = "spec-sheet / inventory"**: mono ProductID corner tag, mono key/value specs `<dl>`, amber price pop, stock color (green/red). `color-scheme: dark`. **Components** (11 .tsx in components/): CatalogApp (root client, motion stagger grid), ProductCard (motion.article, keyboard Enter/Space, role=button), CatalogModal (AnimatePresence, EditForm + DetailPanel dual-mode, WhatsApp icon SVG, CardImage fallback chain), CatalogToolbar (search + select, motion), CatalogStats (3 stat cards, no motion), AdminDashboard (**DEAD — no importer**, superseded by DashboardView), DashboardView (live admin table + CatalogModal edit), LoginForm, ImageUpload (FileReader data-URL preview), Toast (Context provider, 3s auto-hide, role=status aria-live polite), ErrorBanner. **Animation**: framer-motion. Stagger grid (0.06), fade+slide (opacity/y 14-24, dur 0.45, delay 0.1). Card `transition: border-color 0.2s`. Toast keyframe `toast-in` 0.18s. **a11y**: `@media (prefers-reduced-motion: reduce)` kills anim/transition. ProductCard keyboard handler + role=button. Modal: click backdrop closes, inner stopPropagation. Form errors `role="alert"`, toast `aria-live`. **Responsive** breakpoints 1024px (3→2 col grid/stats), 768px (1 col, toolbar stack, form 1 col, login-panel column, admin-table 0.76rem). **Gaps for pro-max**: (1) CatalogStats static no motion (inconsistent with siblings). (2) Login page bare (no brand, no illustration). (3) No skeleton/loading state on SSR fetch — ErrorBanner only on hard fail. (4) WhatsApp CTA gradient green `#1fb86a→#139656` only color with gradient; rest flat — possible inconsistency. (5) Admin table thumb 48x36, upload preview 60x45 — ad-hoc sizes. (6) `image-wrap` aspect 16/10 card vs 16/9 detail — intentional but note. (7) No focus-visible ring (only :focus box-shadow on inputs). -> app/globals.css
