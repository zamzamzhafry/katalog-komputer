# Architecture

> Module depth, seams, dead code, god components. Vocabulary: codebase-design (Module/Interface/Depth/Seam/Deletion Test).

- `2026-06-26` — @claude — Architecture baseline. **Stack**: Next 14.2.5 App Router, React 18.3.1, Supabase SSR+JS, framer-motion ^11.3.19, TS 5.5.4, node --test (experimental-strip-types). **Routes**: `app/page.tsx` (SSR catalog, force-dynamic, fetchCatalogServer), `app/login/page.tsx`, `app/dashboard/page.tsx` (SSR, getUser guard, fetchCatalogServer), `app/actions/catalog.ts` (Server Actions: saveCatalogItem, deleteCatalogItemAction, uploadCatalogImage), `app/layout.tsx`. **Middleware** (middleware.ts): refresh Supabase session cookie per request, protect /dashboard (redirect /login?redirect=), redirect /login→/dashboard if session. **Lib**: `lib/types.ts` (CatalogItem: ProductID/Kategori/Brand/Model/Processor/RAM/Storage/Harga(number)/Stok(number)/FotoURL/Deskripsi), `lib/format.ts` (formatHarga, parseHargaToNumber), `lib/filter.ts`, `lib/edit-state.ts` (toEditState/fromEditState, EditState all-string for form), `lib/storage.ts` (localStorage read/write cache), `lib/drive.ts` (legacy GAS?), `lib/image-url.ts`. `lib/hooks/`: use-catalog-data (SSR items→localStorage cache, categories/summary useMemo), use-catalog-ui (query/category/edit/detail state + actions→Server Actions), use-catalog (EditState type re-export?), use-auth. `lib/supabase/`: admin.ts, auth.ts (signIn/signOut/getUser), catalog.ts (fetchCatalogServer, mapRow), client.ts, database.types.ts, server.ts. **Dead code**: `components/admin-dashboard.tsx` — AdminDashboard has NO importer (grep 0 non-self refs). DashboardView is live equivalent. **Deletion candidate**: remove admin-dashboard.tsx. **Seam quality**: lib/*.ts pure + tested (drive/edit-state/filter/format/image-url .test.ts) — good testability boundary already. UI hooks cleanly separated from data. Server Actions centralize writes. **Ponytail notes found in code**: admin-dashboard.tsx:78 (currentPath null → old-file cleanup skip), image-upload.tsx:65 (data-URL preview eslint-disable). **No god component** (largest ~210 lines DashboardView). Next 14 (not 16) — App Router stable, no RSC suspense boundaries used. -> app/, lib/, components/
